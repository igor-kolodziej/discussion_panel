# Regeneration Wave 2 — Anti-Overmerge Audit

Agent ID: `live-w2-anti-overmerge-01`. Audit clustering only; do not research, score, rank, select, evaluate or rewrite concepts.

Read exactly `regeneration_wave_2/02_candidate_funnel.md` (`56519615c63d57f1df421bbca85b8a883ada5b9a75609a9ae7da620a6ed98786`) plus the five canonical raw-role reports and no other local files. Their hashes are recorded in the cartographer packet.

Check all 50 mappings, with special scrutiny of the three `merged_member` records. A raw concept may share a mechanism family while remaining a `preserved_sibling`; that is not deletion. Require a split only when the current mapping obscures a materially different customer/loss, payer/budget, paid trigger, transaction owner, acquisition route or compounding asset, or when a `merged_member` lacks a field-by-field equivalence rationale. Keep the final retained-direction count inside 10–20. Preserve every raw ID exactly once.

Return: audit verdict; required corrections with exact IDs and replacement mapping if any; confirmation or rejection of each `merged_member`; any missing/duplicate IDs; revised direction and disposition counts; and a concise reconciliation. Do not use or mention shadow output, validation material, scores, thresholds or prior evaluator prose. Return Markdown and do not edit files.
