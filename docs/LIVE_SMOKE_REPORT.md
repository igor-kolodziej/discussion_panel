# Native Workflow Smoke Report

## Result

Run `20260824T180642Z-260b79` completed and published as `no_qualifier`. The
working evaluator returned 3.5. Two fresh native judges, each given only the
same sealed packet and response contract, returned 3.8 and 3.7; the utility
therefore recorded 3.7 as the official native score. The result is a software
success and an honest business non-confirmation.

The complete ignored local run is preserved at
`runs/20260824T180642Z-260b79/`. Its final machine report is
`runs/20260824T180642Z-260b79/final/report.json`, and its human rendering is
`runs/20260824T180642Z-260b79/report.md`.

## Bounded Discovery And Calibration

- Three context-isolated scouts returned one sourced seed each from the
  mandatory-spend, operating-bottleneck, and founder-access lanes. Scouts did
  not receive history, evaluator language, the configured gate, another scout's
  work, or rankings.
- All three exact six-field fingerprints were unique. Delayed semantic review
  nevertheless found that all three retained the broad vertical-software or
  managed-service, partner-distribution, workflow-data pattern.
- The latest legacy v2 pool contained 48 concepts and approximately 31–35
  distinct commercial structures. The recurring diagnostic/service/partner/data
  family appeared in 39 of 48 legacy concepts and all three smoke seeds.
- The sample therefore does not support a quality-improvement claim. It supports
  the value of delayed semantic calibration: exact uniqueness did not hide the
  shared commercial structure.
- The optional diversity repair was exhausted after its two configured
  mechanical attempts rather than opening another generation loop.

## Fault And Retry Evidence

A deliberately malformed scout return was rejected twice, retained as raw
evidence, and left visibly failed after the retry budget. Independent completed
scouts remained usable. The bounded gap-scout job was likewise exhausted
without blocking the run. No job was silently converted to success.

The run exercised `new`, `status`, `resume`, every stage transition, canonical
job completion, `check`, finalization, and publication. Repeated publication was
idempotent. The final event log contains exactly one packet-export event and one
publication event.

## Evidence Triage And Lineage

Delayed history selected `smoke-bottleneck` because the family renewable-energy
company made API coverage, support minutes, truck rolls, remote-resolution rate,
and a paid portfolio pilot comparatively cheap to falsify.

The 12-source research pass materially narrowed the idea from a broad
multi-vendor monitoring platform to a read-only exception desk. It found:

- consumer spending on bundled care, but no evidence of installer software
  willingness to pay or renewal;
- free OEM monitoring and an existing multi-vendor O&M product;
- paid, changing API access and portfolio-access costs;
- low field-service margins relative to software-like assumptions; and
- no evidenced acquisition route or site count capable of supporting the
  founder outcome under conservative economics.

The immutable same-ID lineage is discovery v1 → research v2 → development v3 →
frozen v4. The exact development parent has its own canonical working
evaluation. The frozen version records initial capital as a finite numeric value
and cannot be revised inside this run.

## Holdout And External Import

The shared packet SHA-256 is
`abbc65826c3fd3434edbde94fb66d2b9a003dab1a19d36c84ac060a13d0ee7ea`.
The fresh native responses have distinct raw paths and hashes:

- alpha raw SHA-256:
  `b9f289149be31e1f79148fd949a1b137685bfa55c0b7e03bfc7c11e8027e51ef`;
- beta raw SHA-256:
  `7b811646f47e0044e53babcba82e23ffa94e5c98fc3992dc8421f4257d7095f4`.

Both were recomputed and bound to frozen v4, the canonical rubric snapshot, and
their completed raw jobs. Neither judge saw the configured comparison, working
score, selection history, competing candidates, or the other result.

The manual external path imported an explicit deterministic boundary fixture
from the same response contract. The utility preserved its raw response,
recomputed it exactly at the configured boundary, marked `qualified: false`, and
kept the accepted result binding. The fixture is test provenance, not an
additional market judgment.

## Decision

The final limiter is lack of evidence that independent installers will pay and
renew at economics capable of attractive owner earnings, combined with a low
per-site price and no affordable route to the required portfolio scale.

Reopen only with independent paid pilots and renewals, measured fully loaded
support savings and payback, acceptable diagnostic error rates, brand-weighted
API economics, connected-site retention, and measured cash plus founder-hour
acquisition cost. A later structural revision must enter a new run and receive
fresh judges.

The final report SHA-256 is
`500257b2cb6307839e64d7deb515d8eaecba5076db190b1cdf42739c6caa2e51`.
`python3 scripts/opportunity.py check 20260824T180642Z-260b79` passes with no
source drift.
