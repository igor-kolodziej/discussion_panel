# GStack Workspace State

This directory is intentionally tracked only for reusable setup material.

Runtime files such as `browse.json`, browser sessions, Chromium profiles,
logs, cookies, and local tokens are generated on each machine and must not be
committed. See `BROWSER_STARTUP.md` for the workflow used to start the headed
gstack browser for this workspace.
